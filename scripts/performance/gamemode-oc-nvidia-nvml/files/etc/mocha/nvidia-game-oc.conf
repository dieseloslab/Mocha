# Mocha NVIDIA OC efetivo durante GameMode
# Wayland: aplicar offsets via NVML como root, não via nvidia-settings/Coolbits.
# Aprovado: core +50 / mem +250
CORE_OFFSET=50
MEMORY_TRANSFER_RATE_OFFSET=250
GPU_INDEX=0
