#!/usr/bin/env bash
set -Eeuo pipefail
export LC_ALL=C

if [ "$(id -u)" -eq 0 ]; then
  exec /usr/local/lib/mocha/mocha-kernel-driver-manager-pkexec "$@"
fi

exec pkexec /usr/local/lib/mocha/mocha-kernel-driver-manager-pkexec "$@"
