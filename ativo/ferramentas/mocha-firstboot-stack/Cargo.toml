[package]
name = "mocha-firstboot-stack"
version = "0.2.0"
edition = "2021"
description = "Mocha Stack Optimizer: dynamic CachyOS kernel and NVIDIA stack selector"
license = "MIT"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
panic = "abort"
